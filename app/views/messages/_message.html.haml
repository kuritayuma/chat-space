.contents-right
    .message-user
      = message.user.name
      .time-stamp
        = format_posted_time(message.created_at)
      .comment
        - if message.content.present?
          %p.lower-message__content
            = message.content
        = image tag message.image.url, class: 'lower-message__image' if message.present?
